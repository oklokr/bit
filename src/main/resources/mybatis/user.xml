<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.repository.UserMapper">
    <select id="getUserById" parameterType="string" resultType="com.project.model.UserDto">
        SELECT * FROM members WHERE id = #{id}
    </select>
    <!-- 이메일로 사용자 조회 -->
    <select id="findByCompanyNameAndEmail" resultType="com.project.model.UserDto">
        SELECT * FROM members
        WHERE company_name = #{companyName}
        AND email = #{email}
        LIMIT 1
    </select>
    <!-- 전화번호로 사용자 조회 -->
    <select id="findByCompanyNameAndPhoneNumber" resultType="com.project.model.UserDto">
        SELECT * FROM members
        WHERE company_name = #{companyName}
        AND phoneNumber = #{phoneNumber}
        LIMIT 1
    </select>
</mapper>